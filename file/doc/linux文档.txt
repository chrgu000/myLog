/***************************    部署类   **********/
--查看文件上下10行
grep -C 10 'asdasjdas' 文件名

--回到文件最底位置
shift + G

--查找下一个
N（顺序），shift + N （倒序）


删除当前目录下的文件
rm -f *

解压tar.gz文件的命令是：
tar  -zxvf   压缩文件名.tar.gz





/****************************  运维类  *********/
--对启动脚本添加执行权限
chmod 777  *.sh


--开放端口命令： 
/sbin/iptables -I INPUT -p tcp --dport 8080 -j ACCEPT